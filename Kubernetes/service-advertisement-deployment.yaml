apiVersion: v1
kind: Service
metadata:
  name: lplace-service-advertisement
  namespace: lplace
  labels:
    app: lplace-service-advertisement
    service: lplace-service-advertisement
spec: 
  selector:
    app: lplace-service-advertisement-pod
  ports:
    - protocol: TCP
      name: http
      port: 80
      targetPort: 80
  type: LoadBalancer  
---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: lplace-service-advertisement
  namespace: lplace
spec:
  replicas: 1
  minReadySeconds: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: lplace-service-advertisement-pod
      version: latest   
  template:
    metadata:
      labels:
        app: lplace-service-advertisement-pod
        version: latest
    spec:
      hostname: service-advertisement
      containers:
        - name: lplace-advertisement-container
          image: ghcr.io/luukdekinderen/lplace/service-advertisement:latest
          imagePullPolicy: Always
          envFrom:
            - secretRef:
                name: service-advertisement
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          ports:
            - containerPort: 80

---

apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: lplace-service-advertisement-autoscaler
  namespace: lplace
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: lplace-service-advertisement
  minReplicas: 1
  maxReplicas: 2
  targetCPUUtilizationPercentage: 75