apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-mssql-deployment
  namespace: lplace
spec:
  replicas: 1
  selector:
     matchLabels:
       app: db-user
  template:
    metadata:
      labels:
        app: db-user
    spec:
      terminationGracePeriodSeconds: 30
      hostname: mssqlinst
      securityContext:
        fsGroup: 10001
      containers:
      - name: db-user
        image: mcr.microsoft.com/mssql/server:2019-latest
        ports:
        - containerPort: 1433
        env:
        - name: MSSQL_PID
          value: "Express"
        - name: ACCEPT_EULA
          value: "Y"
        - name: SA_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-user
              key: SA_PASSWORD 
        volumeMounts:
        - name: user-mssql-db
          mountPath: /var/opt/mssql
      volumes:
      - name: user-mssql-db
        persistentVolumeClaim:
          claimName: user-database-volume-claim
---
apiVersion: v1
kind: Service
metadata:
  name: db-user
  namespace: lplace
spec:
  selector:
    app: db-user
  ports:
    - name: db-user
      protocol: TCP
      port: 1433
      targetPort: 1433
  type: ClusterIP